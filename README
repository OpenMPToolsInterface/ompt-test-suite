The test suite comprises of two folders:

regression: which contains a bundle of regression tests to check if an implementation of OMPT is valid or not.
test: a collection of any tests, examples and benchmarks
